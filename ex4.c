#include <stdio.h>

int main(void)
{  
    int raio = 0;
    int diametro, circunferencia, area;
    int PI = 3.1415;

    printf("Digite o valor do raio! ");
    scanf("%d", &);
    
    diametro = raio * 2;
    circunferencia = 2 * PI * raio;
    area = PI * (raio * raio);

    return 0;
}